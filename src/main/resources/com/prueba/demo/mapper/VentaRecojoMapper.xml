<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prueba.demo.mapper.VentaRecojoMapper">

	

	<select id="registrarVentaRecojo" parameterType="com.prueba.demo.core.model.VentaRecojo" statementType="CALLABLE"
			>
		{CALL sp_venta_recojo_ins(
			#{idVentaRecojo, jdbcType = INTEGER, mode = IN},
			#{idVenta, jdbcType = INTEGER, mode = IN},
			#{fecha, jdbcType = DATE, mode = IN},
			#{observacion, jdbcType = VARCHAR, mode = IN},
			#{activo, jdbcType = INTEGER, mode = IN},
			#{usuarioCreacion, jdbcType = INTEGER, mode = IN},
			#{resultado, jdbcType = INTEGER, mode = OUT}
		)
		}
	</select>


</mapper>